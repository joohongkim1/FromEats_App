import React from "react";
// import Papa from 'papaparse'
// import { getStoreListByCategory } from "../../modules/search/search"

// const csvjson = require('csvjson');
// const readFile = require('fs').readFile;
// readFile('./store.csv', 'EUC-KR', (err, fileContent) => {
//     if(err) {
//         console.log(err); // Do something to handle the error or just throw it
//         throw new Error(err);
//     }
//     const jsonObj = csvjson.toObject(fileContent);
//     console.log(jsonObj);
// });

export default function MenuListContainer({ category }) {
  return <div>{/* <Table cols={tripColumns} rows={rows}></Table> */}</div>;
}
